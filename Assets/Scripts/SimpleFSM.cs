using UnityEngine;

public class SimpleFSM : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("I hate Green!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
